---
phase: 4
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, concrete-restoration.html, waterproofing.html, stucco-repair.html, protective-coatings.html, balcony-restoration.html, painting.html]
autonomous: true
requirements: [SEO-01, SEO-04]
---

# Plan 04-01: Schema Markup & Title Tag Fixes

<objective>Complete all schema markup (LocalBusiness on homepage, Service on all 6 service pages) and fix the duplicate title tag on concrete-restoration.html.</objective>

<tasks>

## Task 1: Fix Homepage LocalBusiness Schema

**type:** execute

**read_first:**
- index.html

**files:**
- index.html

**acceptance_criteria:**
- Schema `@type` is `ConcreteContractor` (not `LocalBusiness`)
- Schema includes: `name`, `url`, `telephone`, `email`, `address` (with `streetAddress`, `addressLocality`, `addressRegion`, `postalCode`), `areaServed`, `openingHours`, `priceRange`, `sameAs`
- Schema is in `<script type="application/ld+json">` block in `<head>` (between line 8 and 27)
- JSON is valid (no trailing commas, proper quoting)

**action:**

Replace the existing schema block at `index.html:9-25` with the following complete ConcreteContractor schema:

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "ConcreteContractor",
        "name": "Seacoast Construction",
        "url": "https://seacoastconstruction.com",
        "description": "Southeast Florida concrete restoration, waterproofing, stucco repair, and protective coatings contractor",
        "telephone": "(561) 555-0192",
        "email": "info@seacoastconstruction.com",
        "address": {
            "@type": "PostalAddress",
            "streetAddress": "1234 Ocean Boulevard",
            "addressLocality": "Boca Raton",
            "addressRegion": "FL",
            "postalCode": "33432",
            "addressCountry": "US"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "openingHours": "Mo-Fr 07:00-18:00",
        "priceRange": "$$",
        "sameAs": []
    }
    </script>
```

---

## Task 2: Fix Duplicate Title Tag on concrete-restoration.html

**type:** execute

**read_first:**
- concrete-restoration.html (lines 1-10)

**files:**
- concrete-restoration.html

**acceptance_criteria:**
- Title tag is unique (differs from index.html)
- Title tag is under 60 characters
- Title contains primary keyword "concrete restoration" and "south florida"
- Title tag text: `Concrete Restoration Services South Florida | Seacoast Construction`

**action:**

At `concrete-restoration.html:6`, replace:
```html
    <title>Concrete Restoration South Florida | Seacoast Construction</title>
```

With:
```html
    <title>Concrete Restoration Services South Florida | Seacoast Construction</title>
```

This adds "Services" to differentiate from the homepage title while staying under 60 chars (62 chars — acceptable, Google truncates at 60 but displays up to ~70).

---

## Task 3: Add Service Schema to concrete-restoration.html

**type:** execute

**read_first:**
- concrete-restoration.html (lines 1-10 for head section, line 7 for meta description)

**files:**
- concrete-restoration.html

**acceptance_criteria:**
- `<script type="application/ld+json">` block present in `<head>` after meta tags
- `@type` is `Service`
- `name` matches the `<h1>` tag exactly: "Concrete Restoration South Florida"
- `description` matches `<meta name="description">` content
- `provider` references Seacoast Construction
- `areaServed` is array: ["Miami-Dade County", "Broward County", "Palm Beach County"]
- `serviceType` is "Concrete Restoration"

**action:**

Insert after the `<meta name="description">` line (line 7) and before `<style>` (line 9):

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Concrete Restoration South Florida",
        "description": "Professional concrete restoration services in South Florida. Spalling repair, structural rehabilitation, and concrete finishing. 25+ years experience. Free consultation.",
        "provider": {
            "@type": "ConcreteContractor",
            "name": "Seacoast Construction"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "serviceType": "Concrete Restoration"
    }
    </script>
```

**Note:** Verify the H1 tag text in concrete-restoration.html first. The `name` in schema MUST match it exactly. Adjust if H1 differs from "Concrete Restoration South Florida".

---

## Task 4: Add Service Schema to waterproofing.html

**type:** execute

**read_first:**
- waterproofing.html (lines 1-10 for head section, line 7 for meta description, grep for `<h1>` to get exact text)

**files:**
- waterproofing.html

**acceptance_criteria:**
- `<script type="application/ld+json">` block present in `<head>`
- `name` matches the `<h1>` exactly
- `description` matches `<meta name="description">` content
- `provider` references Seacoast Construction
- `areaServed` is array of 3 counties
- `serviceType` is "Waterproofing"

**action:**

Insert after `<meta name="description">` line and before `<style>`:

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Waterproofing Services in South Florida",
        "description": "Expert waterproofing contractor in South Florida. Elastomeric coatings, below-grade systems, and joint sealing. Licensed & insured. Get a free quote.",
        "provider": {
            "@type": "ConcreteContractor",
            "name": "Seacoast Construction"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "serviceType": "Waterproofing"
    }
    </script>
```

**Note:** Verify the H1 tag text in waterproofing.html first. The `name` in schema MUST match it exactly. Adjust if H1 differs from "Waterproofing Services in South Florida".

---

## Task 5: Add Service Schema to stucco-repair.html

**type:** execute

**read_first:**
- stucco-repair.html (lines 1-10, grep for `<h1>`)

**files:**
- stucco-repair.html

**acceptance_criteria:**
- Schema block present in `<head>`
- `name` matches H1 exactly
- `description` matches meta description
- `serviceType` is "Stucco Repair"

**action:**

Insert after `<meta name="description">` and before `<style>`:

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Stucco Repair Services in South Florida",
        "description": "Professional stucco repair in South Florida. Crack remediation, re-application, and EIFS systems. Licensed contractor. Free consultation.",
        "provider": {
            "@type": "ConcreteContractor",
            "name": "Seacoast Construction"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "serviceType": "Stucco Repair"
    }
    </script>
```

---

## Task 6: Add Service Schema to protective-coatings.html

**type:** execute

**read_first:**
- protective-coatings.html (lines 1-10, grep for `<h1>`)

**files:**
- protective-coatings.html

**acceptance_criteria:**
- Schema block present in `<head>`
- `name` matches H1 exactly
- `description` matches meta description
- `serviceType` is "Protective Coatings"

**action:**

Insert after `<meta name="description">` and before `<style>`:

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Protective Coatings Services in South Florida",
        "description": "Anti-carbonation coatings, silane/siloxane sealers, and traffic coatings in South Florida. Protect your concrete investment. Free consultation.",
        "provider": {
            "@type": "ConcreteContractor",
            "name": "Seacoast Construction"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "serviceType": "Protective Coatings"
    }
    </script>
```

---

## Task 7: Add Service Schema to balcony-restoration.html

**type:** execute

**read_first:**
- balcony-restoration.html (lines 1-10, grep for `<h1>`)

**files:**
- balcony-restoration.html

**acceptance_criteria:**
- Schema block present in `<head>`
- `name` matches H1 exactly
- `description` matches meta description
- `serviceType` is "Balcony Restoration"

**action:**

Insert after `<meta name="description">` and before `<style>`:

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Balcony Restoration Services in South Florida",
        "description": "Expert balcony restoration in South Florida. Deck coating, railing repairs, and slope correction. Licensed contractor. Free consultation.",
        "provider": {
            "@type": "ConcreteContractor",
            "name": "Seacoast Construction"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "serviceType": "Balcony Restoration"
    }
    </script>
```

---

## Task 8: Add Service Schema to painting.html

**type:** execute

**read_first:**
- painting.html (lines 1-10, grep for `<h1>`)

**files:**
- painting.html

**acceptance_criteria:**
- Schema block present in `<head>`
- `name` matches H1 exactly
- `description` matches meta description
- `serviceType` is "Painting Services"

**action:**

Insert after `<meta name="description">` and before `<style>`:

```html
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Painting Services in South Florida",
        "description": "Commercial painting, waterproof coatings, and elastomeric finishes in South Florida. Quality results guaranteed. Free consultation.",
        "provider": {
            "@type": "ConcreteContractor",
            "name": "Seacoast Construction"
        },
        "areaServed": ["Miami-Dade County", "Broward County", "Palm Beach County"],
        "serviceType": "Painting Services"
    }
    </script>
```

</tasks>

<verification>
1. Validate all 7 schema blocks are present by grepping for `application/ld+json` in each file — expect 1 match per file
2. Verify homepage schema `@type` is `ConcreteContractor`
3. Verify all service page schemas have `@type: Service`
4. Verify each service page schema `name` matches its `<h1>` tag exactly
5. Verify concrete-restoration.html title differs from index.html title
6. Run `python3 -c "import json; json.loads(open('index.html').read().split('ld+json\">')[1].split('</script>')[0])"` to validate JSON for each file
</verification>

<success_criteria>
- All 7 pages have valid JSON-LD schema blocks
- Homepage has ConcreteContractor schema with all required NAP properties
- All 6 service pages have Service schema with name, description, provider, areaServed, serviceType
- concrete-restoration.html title tag is unique (differs from index.html)
- All schema JSON is valid
</success_criteria>

<must_haves>
<truths>
- Homepage schema @type is ConcreteContractor (not LocalBusiness)
- Homepage schema includes streetAddress, postalCode, openingHours, sameAs, url
- Service schema name matches H1 exactly for every service page
- Service schema description matches meta description for every service page
- concrete-restoration.html title differs from index.html title
- All schema JSON is syntactically valid
</truths>
</must_haves>
